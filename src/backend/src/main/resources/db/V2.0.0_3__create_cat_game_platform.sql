CREATE TABLE cat_game_platform (
     name text PRIMARY KEY,
     release_date text,
     developer TEXT,
     manufacturer TEXT,
     cpu TEXT,
     memory TEXT,
     graphics TEXT,
     sound TEXT,
     display TEXT,
     media TEXT,
     max_controllers TEXT,
     notes TEXT
);